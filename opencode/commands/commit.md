---
description: Commit changes without pushing
agent: build
---

Review the current changes using git status and git diff.
Create a meaningful commit message based on the changes.
Commit the changes (do NOT push to remote).
Make sure to follow the repository's commit message conventions by looking at recent git log history.
